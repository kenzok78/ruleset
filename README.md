### ruleset

用于 Clash / Mihomo / HomeProxy 的规则与配置模板。

推荐模板：

- `rule/config/Clash/kenzo11_twitter_public.yaml`（已去除个人订阅，使用 `机场1/订阅1` 占位）
- `rule/config/Clash/config.yaml`（单订阅基础模板）

说明：

- 本仓库模板不包含真实订阅链接。
- 使用前请将 `订阅1/订阅2/订阅3` 替换为你自己的订阅地址。
